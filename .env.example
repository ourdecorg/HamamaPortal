# Hamama runs fully without any environment variables.
#
# OPTIONAL — reserved for a future LLM-backed discovery provider.
# The app does not read this today; see getDiscoveryProvider() in lib/discovery.ts
# for the single place where a model-backed provider would be plugged in.
# ANTHROPIC_API_KEY=
